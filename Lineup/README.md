# System Analysis
Run LineUP_code.m for the old lineup (ADC peak power = -10dBm)
Run LineUP_code_ADC_mod.m for the  modified lineup (ADC peak power = 14dBm)
Add the achieved specs to the lineup to see the new results , if number of blocks is not enough
you can cascade you blocks analytically using qorvo then add the results to the code
